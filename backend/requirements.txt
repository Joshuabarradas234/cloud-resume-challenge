# Runtime: boto3 is provided by the Lambda Python runtime, so it is NOT
# packaged into the deployment zip. It is listed here only so the test job can
# import the handler.
boto3>=1.34

# Test-only.
moto[dynamodb]>=5.0
pytest>=8.0
